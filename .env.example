# 复制为 .env.local 并填入 Supabase 项目设置（Project Settings → API）
# 未配置时应用仍可用本地 `src/data/locations.ts` 与 localStorage 运行。

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
